-- Seed is applied by `supabase db reset` (local) after migrations.
-- Prefer `make -C api seed` which upserts from the same Go stub data used
-- when API_DATABASE_URL is unset, so content stays in one place.
--
-- This file is intentionally a no-op placeholder so `supabase db reset`
-- succeeds before the Go seed has been run.
SELECT 1;
